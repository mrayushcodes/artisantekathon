import { useState } from "react";
import { Link } from "react-router-dom";
import { ArrowLeft, ArrowRight, Check, ChevronDown, ImagePlus, Mic, Play, Sparkles, Upload, WandSparkles } from "lucide-react";

const steps = ["Add a photo", "Tell your story", "Create details", "Set your price", "Preview"];

export default function Studio() {
  const [step, setStep] = useState(0);
  const [processing, setProcessing] = useState(false);
  const [image, setImage] = useState<string | null>(null);
  const [language, setLanguage] = useState("Hindi");
  const [published, setPublished] = useState(false);

  const next = () => {
    if (step === 1) {
      setProcessing(true);
      window.setTimeout(() => { setProcessing(false); setStep(2); }, 1300);
    } else setStep((current) => Math.min(current + 1, steps.length - 1));
  };

  return <div className="min-h-screen bg-paper text-ink"><header className="border-b border-line bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 lg:px-8"><Link to="/" className="flex items-center gap-2 text-xl font-extrabold tracking-[-0.06em]"><span className="grid h-8 w-8 place-items-center rounded-full bg-saffron text-white"><Sparkles size={15} /></span>artisan<span className="text-saffron">AI</span></Link><div className="flex items-center gap-3 text-sm"><span className="hidden text-muted sm:block">Saving automatically</span><div className="avatar bg-[#D99561]">AS</div></div></div></header><main className="mx-auto max-w-6xl px-6 py-10 lg:px-8"><div className="mb-10 flex items-center gap-3 text-sm text-muted"><Link to="/" className="hover:text-ink"><ArrowLeft className="mr-1 inline" size={15} /> Home</Link><span>/</span><span className="font-semibold text-ink">New product</span></div><div className="mb-10"><div className="eyebrow">Your new listing</div><h1 className="text-4xl font-extrabold tracking-[-0.05em] sm:text-5xl">Bring your craft to life.</h1><p className="mt-3 text-muted">A few simple steps. You can change everything before you publish.</p></div><div className="mb-12 flex items-start justify-between gap-2 border-b border-line pb-6">{steps.map((label, index) => <div key={label} className="flex flex-1 items-start gap-2"><div className={`grid h-8 w-8 shrink-0 place-items-center rounded-full text-xs font-bold ${index < step ? "bg-saffron text-white" : index === step ? "bg-ink text-white" : "bg-cream text-muted"}`}>{index < step ? <Check size={15} /> : index + 1}</div><div className="hidden pt-1 text-xs font-bold sm:block">{label}</div></div>)}</div>{published ? <div className="mx-auto max-w-2xl rounded-[28px] border border-line bg-white p-10 text-center shadow-sm"><div className="mx-auto mb-5 grid h-16 w-16 place-items-center rounded-full bg-[#E5F4E7] text-green-700"><Check size={30} /></div><div className="eyebrow">Published successfully</div><h2 className="mt-2 text-3xl font-extrabold tracking-tight">Your basket is now discoverable.</h2><p className="mx-auto mt-4 max-w-md leading-7 text-muted">Buyers can find your new listing and request a bulk quote. You can edit it anytime from your dashboard.</p><Link to="/" className="mt-8 inline-block rounded-full bg-ink px-6 py-3.5 text-sm font-bold text-white">Back to marketplace <ArrowRight className="ml-1 inline" size={15} /></Link></div> : <div className="grid gap-8 lg:grid-cols-[1.15fr_0.85fr]"><div className="rounded-[28px] border border-line bg-white p-6 sm:p-8">{step === 0 && <><h2 className="text-2xl font-extrabold tracking-tight">Start with a product photo</h2><p className="mt-2 text-sm leading-6 text-muted">A clear photo helps AI understand your craft. You can improve it later.</p><label className={`mt-7 flex min-h-[360px] cursor-pointer flex-col items-center justify-center overflow-hidden rounded-3xl border-2 border-dashed ${image ? "border-saffron bg-cream" : "border-line bg-paper hover:border-saffron"}`}>{image ? <img src={image} alt="Uploaded product" className="h-full max-h-[360px] w-full object-cover" /> : <><div className="mb-5 grid h-16 w-16 place-items-center rounded-2xl bg-cream text-saffron"><Upload size={25} /></div><div className="font-bold">Drop a photo here</div><div className="mt-2 text-sm text-muted">or click to browse from your device</div><div className="mt-6 flex gap-2"><span className="chip">JPG</span><span className="chip">PNG</span><span className="chip">Max 10MB</span></div></>}<input type="file" accept="image/*" className="hidden" onChange={(event) => { const file = event.target.files?.[0]; if (file) setImage(URL.createObjectURL(file)); }} /></label>{image && <button className="mt-4 flex items-center gap-2 text-sm font-bold text-saffron"><WandSparkles size={16} /> Enhance with AI</button>}</>}{step === 1 && <><h2 className="text-2xl font-extrabold tracking-tight">Tell us about your product</h2><p className="mt-2 text-sm leading-6 text-muted">Speak naturally. We’ll turn your words into a professional catalog.</p><div className="mt-7 flex items-center justify-between rounded-2xl bg-cream px-4 py-3"><span className="text-sm font-semibold">I’m speaking in</span><button className="flex items-center gap-2 rounded-lg bg-white px-3 py-2 text-sm font-bold">{language}<ChevronDown size={14} /></button></div><div className="my-10 text-center"><button className="mx-auto grid h-28 w-28 place-items-center rounded-full bg-saffron text-white shadow-xl shadow-saffron/20 transition hover:scale-105"><Mic size={38} /></button><div className="mt-5 text-sm font-bold">Tap to start recording</div><div className="mt-1 text-xs text-muted">You can also upload an audio recording</div></div><div className="flex items-center justify-between rounded-2xl border border-line p-4"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-sky"><Play size={15} /></div><div><div className="text-sm font-bold">Your voice note</div><div className="text-xs text-muted">00:18 · {language}</div></div></div><button className="text-xs font-bold text-saffron">Record again</button></div><div className="mt-5 rounded-2xl bg-sky p-4"><div className="mb-2 text-xs font-bold uppercase tracking-widest text-muted">Transcript</div><p className="text-sm leading-6">“Yeh Assam ka bamboo basket hai. Isko hum haath se banate hain. Iska size 12 inch hai...”</p></div></>}{step === 2 && <><h2 className="text-2xl font-extrabold tracking-tight">Your product details are ready</h2><p className="mt-2 text-sm leading-6 text-muted">AI made a first draft. Read it over and make it yours.</p><div className="mt-7 space-y-5">{[["Product name", "Handmade Assam Bamboo Basket"], ["Category", "Handicrafts · Bamboo Craft · Basketry"], ["Material", "Bamboo"], ["Made in", "Assam, India"], ["Size", "12 inches"]].map(([label, value]) => <label key={label} className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-widest text-muted">{label}</span><input defaultValue={value} className="w-full rounded-xl border border-line bg-paper px-4 py-3 text-sm font-semibold outline-none focus:border-saffron" /></label>)}<label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-widest text-muted">English description</span><textarea defaultValue="A beautifully handcrafted bamboo basket made by skilled artisans from Assam. Lightweight, durable and woven with care for everyday use or thoughtful gifting." className="min-h-28 w-full resize-none rounded-xl border border-line bg-paper px-4 py-3 text-sm leading-6 outline-none focus:border-saffron" /></label><div className="rounded-2xl bg-sky p-4 text-sm leading-6"><strong>Hindi description</strong><p className="mt-1">असम के कुशल कारीगरों द्वारा हाथ से तैयार की गई सुंदर बाँस की टोकरी।</p></div></div></>}{step === 3 && <><h2 className="text-2xl font-extrabold tracking-tight">Set a fair price</h2><p className="mt-2 text-sm leading-6 text-muted">Your recommendation is based on the time, material and effort that goes into your work.</p><div className="mt-7 grid gap-4 sm:grid-cols-2">{[["Material cost", "₹180"], ["Labour cost", "₹100"], ["Other cost", "₹30"], ["Time to make", "2 hours"]].map(([label, value]) => <label key={label} className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-widest text-muted">{label}</span><input defaultValue={value} className="w-full rounded-xl border border-line bg-paper px-4 py-3 text-sm font-semibold outline-none focus:border-saffron" /></label>)}</div><div className="mt-6 rounded-3xl bg-ink p-6 text-white"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-white/50"><Sparkles size={14} className="text-saffron" /> AI-assisted recommendation</div><div className="mt-5 flex items-end justify-between"><div><div className="text-3xl font-extrabold">₹450–₹550</div><div className="mt-1 text-sm text-white/50">suggested retail price</div></div><div className="text-right"><div className="text-xl font-bold text-saffron">₹420–₹460</div><div className="mt-1 text-xs text-white/50">B2B range</div></div></div><div className="mt-5 border-t border-white/10 pt-4 text-xs leading-5 text-white/55">Why this price? Material cost, 2 hours of labour, category demand and sample market references.</div></div></>}{step === 4 && <><h2 className="text-2xl font-extrabold tracking-tight">Preview your listing</h2><p className="mt-2 text-sm leading-6 text-muted">This is how buyers will see your product.</p><div className="mt-7 overflow-hidden rounded-3xl border border-line"><div className="h-64 bg-cream">{image ? <img src={image} alt="Basket preview" className="h-full w-full object-cover" /> : <div className="grid h-full place-items-center text-muted"><ImagePlus size={35} /></div>}</div><div className="p-5"><div className="mb-2 text-xs font-bold uppercase tracking-widest text-saffron">Handicrafts · Assam</div><h3 className="text-2xl font-extrabold tracking-tight">Handmade Assam Bamboo Basket</h3><p className="mt-3 text-sm leading-6 text-muted">A beautifully handcrafted bamboo basket made by skilled artisans from Assam. Lightweight, durable and woven with care.</p><div className="mt-5 flex items-center justify-between border-t border-line pt-5"><div><div className="text-xl font-extrabold">₹450–₹550</div><div className="text-xs text-muted">MOQ 10 units</div></div><span className="chip">AI catalog ready</span></div></div></div></>}</div><aside className="h-fit rounded-[28px] bg-cream p-6 sm:p-8"><div className="mb-5 flex items-center gap-2 text-xs font-bold uppercase tracking-[0.16em] text-muted"><Sparkles size={15} className="text-saffron" /> ArtisanAI guide</div>{processing ? <div className="py-10"><div className="mx-auto mb-6 grid h-16 w-16 animate-pulse place-items-center rounded-full bg-saffron text-white"><WandSparkles size={25} /></div><div className="space-y-3 text-center text-sm font-semibold"><div>Understanding product...</div><div className="text-muted">Identifying material...</div><div className="text-muted">Preparing catalog...</div></div></div> : <><div className="text-lg font-bold">{step === 0 ? "A great listing starts with one good photo." : step === 1 ? "There’s no wrong way to tell your story." : step === 2 ? "Everything here stays editable." : step === 3 ? "Your work deserves a fair price." : "Ready to meet your next buyer?"}</div><p className="mt-3 text-sm leading-6 text-muted">{step === 0 ? "Don’t worry about perfect photography. Our image studio can help clean up the background and lighting." : "Use your everyday language. Hindi, Bengali, Tamil and more are welcome."}</p><div className="mt-7 rounded-2xl border border-line bg-white p-4 text-sm"><div className="font-bold">Demo AI</div><div className="mt-1 leading-6 text-muted">You’re using demo mode. Connect Gemini later for live image and language intelligence.</div></div></>}</aside></div>}{!published && <div className="mt-8 flex justify-between gap-3"><button onClick={() => setStep(Math.max(step - 1, 0))} disabled={step === 0} className="rounded-full border border-line bg-white px-5 py-3 text-sm font-bold disabled:opacity-30">Back</button><button onClick={() => step === 4 ? setPublished(true) : next()} className="rounded-full bg-ink px-6 py-3 text-sm font-bold text-white hover:bg-saffron">{step === 0 ? "Continue" : step === 1 ? (processing ? "Creating catalog..." : "Create details with AI") : step === 4 ? "Publish product" : "Continue"} <ArrowRight className="ml-1 inline" size={15} /></button></div>}</main></div>;
}
